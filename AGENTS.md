## Agent skills

### Issue tracker

Issues are tracked using GitHub Issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Using standard default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout at the repository root. See `docs/agents/domain.md`.