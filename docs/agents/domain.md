# Domain Docs Configuration

**Layout:** Single-context

Agents should assume the following structure to learn domain language and architectural decisions:
- `CONTEXT.md` is located at the repository root.
- Architecture Decision Records (ADRs) are located in `docs/adr/`.
